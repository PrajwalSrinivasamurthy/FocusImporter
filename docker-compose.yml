services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: focus-importer
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - logs:/app/logs
      - ./data:/app/data
    restart: unless-stopped

volumes:
  logs:
